# 博客文档写作规则

## 内容组织

- **孤零零的知识点不应挂在某节末尾**：如果一段内容只在为另一个章节提供前提/背景，它应该融合到那个章节里，而不是孤零零地附在当前章节末尾。读者在当前章节看不到它的关联性，读到目标章节时又已经忘了它。

## Markdown 格式

- `- ` 列表如果连续多行，第一个 `- ` 前面必须加一个空行，否则不会自动换行，会紧贴前面的文字。
- `> ` 引用块要实现换行的话，两行之间必须插多一个 `> `（空引用行），否则不会换行。
- spec/标准文档的引用（ARM规则 ID、SMMU spec 章节等）不要把英文原文整段堆在正文里。正文中只写：**粗体规则 ID** + 一句话中文要点（括号注明"原文见附录X"）；英文原文 + 中文翻译统一收录到文末"附录"。
- 正文行文不要大段大段地堆叠文字。多个想法挤在一段里时：
  - 要么拆成 `- ` 分点列表（每点只讲一件事，如果依然有内容冗余，可以加个二级的`-`，以此类推，分级沉淀一大段描述）
  - 要么压缩成一句话；超过两句话的解释就应该考虑分点。
  - spec 引用、代码块、表格、图不属于"大段堆叠"。

## 语言表达

- **意简言骇**：不写废话，不重复陈述同一观点。每个段落只讲一件事，层层递进。
- **不要自行发明名词**：使用标准术语，不要创造"软件收口"这类非标准表述。如果需要描述一个概念，用已有的技术术语或用人话直白描述。
- **加粗要精准**：关键的地方加 **加粗**，但不能频繁地加，物极必反。
- **标题是精炼的知识点**：不是提问，不要把用户提问的内容直白地写出来，而是整理成一个知识点展示出来。

## 文档修改

- **静默修改**：改了啥就是啥，不要在正文中汇报"你之前是 X，现在改成 Y"。
- **图片是易读的**：重构后图片要尽可能保留。不要随意修改 plantuml 图的源码（可能渲染失败）。
- **图片编号联动**：图片放置顺序如果修改的话，有编号的图片，编号也得修改。
- **动态插入**：新内容要整理清楚文章逻辑，思考应该在哪一个位置添加哪些内容，动态插入到文档中，而不是无脑地添加到文章末尾。
- **尊重用户调整**：如果用户对之前 Accept 的内容做了手动调整，下次写入其他内容时不要自作主张改回原来 accept 的内容，而是提醒用户让用户自己决定。
- **合并重复**：文章中有多处重复的内容，整理总结成一处，然后找到最合适的位置去写。

## 重构流程

- 遍历全文，总结最好的文档结构，内容分阶段说明。
- 一开始先定义好概念，不要自行发明名词。
- 写或改文件前先通读，找到各处冗余重复的内容，整理总结合并成一处，然后找到最合适的位置去写。
- 保证内容完整，逻辑合理，方便阅读。
- 文章表述方式要让人的可读性高一些。
- 如果重构到后面的章节，review 后发现有一些内容应该合并到前面，需要更新插入到前面的内容中。
